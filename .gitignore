# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Original avatar sources (only optimized files in public/images/avatars/ are committed)
# Folder is kept in repo via .gitkeep; only contents (large PNGs) are ignored
scripts/source-avatars/*
!scripts/source-avatars/.gitkeep
